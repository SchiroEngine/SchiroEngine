//! Input event types.
//!
//! Will define the strongly typed events emitted by the input system
//! (key down, button pressed, axis changed, ...) so that downstream
//! code can react without inspecting raw `WindowEvent`s.
