//! Kira based audio engine wrapper.
//!
//! This module is the home of the `kira::AudioManager` once the
//! integration lands. For now it only documents the intended surface
//! area so that downstream code can already reference the module.
