//! Read-only inspector used when no entity is selected.
//!
//! The interactive inspector lives in [`crate::panels::inspector`]; the
//! placeholder here exists for backwards compatibility and will be
//! removed once the full inspector covers every case.
